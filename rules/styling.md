---
alwaysApply: true
---

# Styling Rules

## Component Styling Rule
- **ALWAYS** use Tailwind CSS classes for layout and utility styling. The classes should use the theme properties e.g `text-foreground`, `text-muted-foreground` `border-border` `bg-background`
- **NEVER** create inline styles unless absolutely necessary